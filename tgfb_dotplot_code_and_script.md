# TGFB 신호전달 경로 군집별 발현 시각화 발표 자료

## PPT에 게시할 코드

```r
# 1. 분석을 위한 필수 라이브러리(Seurat 및 시각화용 ggplot2) 로드
library(Seurat)
library(ggplot2)

# 2. TGFB 신호전달 경로 관련 주요 인자(TGFB1 및 수용체)의 군집별 발현 양상 시각화
# - TGFB1: 주요 신호 전달 리간드
# - TGFBR1, TGFBR2: 해당 리간드에 반응하는 수용체
DotPlot(merged_seurat, features = c("TGFB1", "TGFBR1", "TGFBR2")) +
  theme(axis.text.x = element_text(angle = 45, hjust = 1))
```

## 발표 대본

이 코드는 `merged_seurat` 객체를 바탕으로, TGFB 신호전달 경로와 관련된 주요 유전자인 `TGFB1`, `TGFBR1`, `TGFBR2`의 발현 양상을 세포 군집별로 시각화하기 위한 코드입니다. 목적은 특정 군집에서 TGFB 신호전달 리간드와 수용체가 얼마나 발현되는지를 한 번에 비교하여, 어떤 군집이 이 신호전달 경로에 더 적극적으로 관여할 가능성이 있는지를 확인하는 것입니다. 먼저 `library(Seurat)`와 `library(ggplot2)`를 불러옵니다. `Seurat`는 단일세포 RNA-seq 데이터 분석과 시각화에 사용되는 핵심 패키지이고, `ggplot2`는 그래프의 테마와 축 레이블 등 시각적 요소를 조정하는 데 사용됩니다.

이후 핵심 실행 부분은 `DotPlot(merged_seurat, features = c("TGFB1", "TGFBR1", "TGFBR2"))`입니다. 여기서 `DotPlot()`은 Seurat 객체에 포함된 세포 군집들을 기준으로, 지정한 유전자들의 발현 패턴을 점 그래프 형태로 보여주는 함수입니다. `features = c("TGFB1", "TGFBR1", "TGFBR2")`는 분석할 유전자를 지정하는 부분으로, `TGFB1`은 TGFB 신호전달의 대표적인 리간드이고, `TGFBR1`, `TGFBR2`는 그 리간드에 반응하는 주요 수용체입니다. 따라서 이 세 유전자를 함께 보면, 특정 군집이 TGFB 신호를 분비하는지, 혹은 그 신호를 수용할 준비가 되어 있는지를 동시에 해석할 수 있습니다.

DotPlot에서 각 점의 크기는 일반적으로 해당 군집 내에서 그 유전자를 발현하는 세포의 비율을 의미하고, 점의 색은 평균 발현량의 상대적 수준을 의미합니다. 따라서 점이 크고 색이 진하면, 그 군집에서 해당 유전자가 더 많은 세포에서 높게 발현된다고 해석할 수 있습니다. 반대로 점이 작고 색이 옅으면, 그 유전자를 발현하는 세포 비율이 낮거나 평균 발현량이 낮다는 뜻입니다. 마지막으로 `theme(axis.text.x = element_text(angle = 45, hjust = 1))`는 x축에 표시되는 유전자 이름이 겹치지 않도록 45도 기울여 표시하는 설정입니다. 정리하면, 이 코드는 TGFB 신호전달 경로의 핵심 리간드와 수용체가 각 군집에서 어떻게 발현되는지를 비교하여, 특정 군집의 신호전달 특성을 해석하기 위한 시각화 코드입니다. 핵심 의의는 하나의 그래프 안에서 여러 유전자의 발현 강도와 발현 세포 비율을 동시에 확인할 수 있어, TGFB 경로의 활성 가능성을 군집 단위로 직관적으로 파악할 수 있다는 점에 있습니다.
